# Bag End — Task Ledger

Roth's ledger. Status legend: OPEN (ready to work) · BLOCKED (waiting on
another task) · OWNER (waiting on Joe) · PARKED (owner set aside) · DONE.
Renumbering only with Joe's nod.

| # | Task | Status | Notes |
|---|---|---|---|
| 1 | **Schema v0.2** for `books.db` — single-threaded authorship (COS), fix order: identity model → state precedence by declared source recency → allow-list storage + column maps → cheap invariants → only then missing tables | **DONE — ratified v0.2.1** (2026-09-07) | DDL at `tools/schema/books.sql`; blueprint in `docs/superpowers/plans/2026-09-07-schema-v0.2.md`; convergence `.scratch/schema-review/v02/CONVERGENCE.md`; two harnesses green (49 + 26 assertions) |
| 2 | Three-leg panel re-review of v0.2 — clean verdict required before any fact rows land | **DONE** | Round 2 verdicts: CLEAN · CLEAN · CLEAN-WITH-NITS (the round-1 REJECT leg re-executed its own probes and returned CLEAN) |
| 3 | Resolve the 11 `header_row: unresolved` tabs — propose with evidence, Joe confirms before ingest | **DONE — settled with the owner (2026-09-11); 0 unresolved entries remain** | 3 independent flash legs (n/n on 9 of 11) + COS spot-verification at source, then per-tab owner rulings. Written into `private/layouts/layouts.json`; proposal/history at `private/layouts/task3-proposal-2026-09-10.md`. Resolved: `Inv Income` (two-tier + derived E/J/O/P/Q/S/T), `401k Contributions` (`H` canonical, `R` duplicate; catch-up from 2019), `Taxes` (4 group bands; `E`/`H` are cross-sheet pulls), `NEAR` (7 vertical merges; totals inside header cells), `Tax Rates` (series starts 2015; rows 2010-2014 are pre-series labels), and exclusions — `Buybacks` (worksheet), `Sheet1`/`Sheet2` (tabs deleted by owner), `2026 Data` (scratch), `Social Security Earnings` (derived, feeds Chart2), `VCA_PLAN` (duplicate of `Row Data`). Method: the new native **structure** read (`sheets grid`) was what made this settleable. Follow-on loader work filed as #18. COLA removed by owner ruling (public data, web-pullable) |
| 4 | Apply v0.2.1 to a fresh store; **loader contract** then **wave-1 ingest** (spine → brokerage ledgers → SSA → checking/card); build the first decumulation answer to Q1 | **WAVE-1 DONE** — 4,342 current fact rows on a fresh v0.2.1 store: spine 1,045 cells (incl. derived LTC + IRS metrics) · events 3,213 across brokerage/checking/card, 99.9% classified (2 sentinel rows) · SSA 27 + 56. Loader battery 23 assertions green; clean rebuild reproducible. **Q1 ANSWERED** (owner rulings: 2× spending + ~1yr-income package; retire now = resounding yes; all variants cleared except the harshest no-SS case at ~68) | Q1 = "can I retire now?" |
| 5 | **Charter v1.0** — `AGENTS.md` (ratified 2026-09-07), `README.md`, `docs/TASKS.md` (this file), `docs/CODEBASE_OVERVIEW.md`; `to-be-*` drafts retired to `.scratch/retired/` | **DONE — ratified** | plain-language pass + panel rulings folded into v1.0 changelog |
| 6 | Owner rulings queue: spending level the plan must fund · target stop age + SS@67 decision-or-variable · five money questions · zones & inventory design · D9/D13 cosmetics | OWNER | **commit/remote posture RESOLVED 2026-09-08: first commit a3866b2 pushed to origin/main (owner-directed)**; none block 1–4 |
| 7 | **Complete the layouts allow-list** for the `trading-*` sheets' unlisted tabs (per-account state tabs, `Stock P&L`, `TIPS`, `NEAR`, `Data`, transaction tabs, scratch tabs) — fan-out from `.scratch/survey/b1*.txt` evidence; COS authors entries; Joe confirms headers | OPEN | found by Roth's 2026-09-06 tab census; source-level notes added to `private/layouts/layouts.json` |
| 9 | **Wave-2: holdings history** — **complete monthly series: 372 statement PDFs parsed, TD brokerage account 189 months 2008-12 -> 2026-09** (statement parsers, folder-qualified fetch, account auto-routing, verified to printed totals); cost-basis bridge as state; USBANK LLC card statements loaded (entity=LLC) | **DONE** | Gaps documented: plan-custodian 2008-2016 numbered statements have no structured qty/cost text layer (Rollover per-security history needs the custodian's CSV exports — ruling requested); card image-only PDFs (CSV export ruling); per-lot FIFO lives in the custodian's lot ledger |
| 10 | **Q2 groundwork** — 401(k) plan ledgers loaded (2,124 rows, fully classified incl. informational markers, cross-file dedupe with evidence); IRS 2026 brackets in `dim_tax_bracket` (IR-2025-103); Q2 assumptions registered (filing status + other income NEEDS-VERIFY); first conversion-ladder answer computed (`.scratch/q2/roth_path.py`) | **FIRST ANSWER DELIVERED** — Plan A (12% bracket, 2026-2035) converts 504k at ~58k federal tax; Plan B (22%) converts 1.06M at ~124k; no-conversion RMDs land in 24%+; **Q2 refresh (tax-history fold-in) PARKED per owner 2026-09-08** | refinements queued when reopened: filing status confirmed Single by the 2026-09-08 tax scan (assumption already satisfied), SS-taxation interaction, bracket indexing, NJ state tax, no-state-tax move |
| 11 | **Full tax-return scan 2012-2025** — fetch + parse IRS transcripts (2019-2022) and tax-prep Records/Filing PDFs (2012-2017, 2023-2025) into `tax_year_facts`; cross-check vs Joe's Tax Rates history; panel-verified | **DONE** (2026-09-08) — schema v0.2.4 (5 tax line-item columns, additive, parity-verified); 14 year rows loaded in batch 31 under dedicated tax sources; 4-leg flash verification n/n zero discrepancies; all three owner anchors match (2015, 2020, 2022); **2018 filled 2026-09-08 (batch 32) from the Vault-recovered Records PDF + Account Transcript witness — history now complete 2012-2025** | 2022 marginal_bracket '22%' retired (was an effective rate, 21.9%); 2023-2025 filing status derived (needs_verify=1); **Schedule C forms are blank in every fetched PDF → LLC loss history not in these sources — stays OWNER-gated** |
| 12 | **Q3 — portfolio optimization** (scope per owner 2026-09-08): tax efficiency, asset location, withdrawal sequencing, structure, concentration *policy*. **Investment selection is OUT of scope — deferred to the 10x repo.** | **PARKED** (owner, 2026-09-08) | Deliverable when reopened: decision memo — each asset's named job, sequencing order, tax-location analysis on Joe's filed rates (15% LTCG + 3.8% NIIT, 32→35% ordinary), sleeve deployment rule, concentration policy options with triggers/falsifiers |
| 13 | **Financial page (dashboard)** — present the store's vital data to Joe: retirement dashboard first (owner directive 2026-09-08, architecture B) | **DONE — LIVE** | Exporter: `bagend.py export finpage` → protocol-2 JSON; static page `apps/finpage/BagEnd.html` (Connect pattern, zero deps/numbers, JS/Python parity harness). **Owner ruling: managed in THIS repo only; hosted on its own Pages site (root index.html + `.nojekyll`); homepage links to it; data stays in Homepage-DB/BagEnd/. Live at the repo's Pages URL — persistence verified 2026-09-08.** |
| 15 | **Dashboard framework** — owner ruling 2026-09-08: one capability = one page, shared shell (Connect flow, theme, nav) + shared model core + one data contract | **DONE** (2026-09-08) — `apps/finpage/src/` (shell + core.css/core.js + pages.json registry + pages/retirement.js) + `build.py` inliner → single-file `apps/finpage/BagEnd.html`; payload v2 (`pages.*` sections, one Connect for all pages); parity harness green | Add a capability = add a page module + registry entry + payload section, rebuild |
| 16 | **Withdrawal-simulation page** (owner example 2026-09-08): IRS withdrawal sim — RMD schedule (age 73), account-bucket withdrawals, tax at filed rates, Q2 conversion Plans A/B interactive | **LONG-TERM SHELF** (owner: broad effort; current phase = data collection) | Riding #15 when reopened: `position_lot`/holdings per account, `tax_year_facts` rates, `dim_tax_bracket`, Q2 scripts |
| 17 | **Data-collection phase** (owner directive 2026-09-08: enrich books.db before broad features) — IRS payments · card detail 2022+ · lot cost basis 2019-2021 · Task #3 header proposals · owner CSVs · Schedule C source · meter readings | **IN PROGRESS** — IRS payments loaded (batch 33, 4 years); card statements loaded (batch 34: 2022 + 2025 from the primary card, plus a new Visa card 2026 = 1,173 rows, spot-verified to printed statement totals, exact); **lots 2019-2021 CLOSED as a finding** (no per-lot detail in the corpus — broker sites only; position-level cost basis already in the store) | Next: Task #3 proposals · owner CSVs (card + plan-custodian) · Schedule C source · meter readings |
| 14 | **2018 IRS transcript fetch** — closes the only tax-history hole | **DONE** (2026-09-08) — fetched Records PDF + Account Transcript + Wage & Income from the recovered Tax Records tree; loaded batch 32; dual-witness verified (Records summary + Account Transcript agree on taxable 172,454 and Single) | Tax history 2012-2025 now complete — zero holes |
| 8 | Decumulation substrate design: derive `A_max` (the zero-at-age-100 ceiling withdrawal) instead of typing it; run the ladder at ceiling **and** planned-spending levels; SS base case = 78% of scheduled (2026 Trustees Report); 2032 step-change for claim-at-62 analysis | **FIRST PASS DELIVERED** (2026-09-08) — `.scratch/q8/annuity_max.py`: A_max goal-seeked from the store (balance, SS@62/@67, assumptions) per variant × yield; ceiling ≈1.5-1.76× planned spending; at plan the ladder never decumulates. Pending: escalation variants, taxes in the ladder, page integration (#13) | Retirement Balance decoded + owner-confirmed 2026-09-06 |
| 18 | **Loader: consume the new allow-list semantics** — teach `loader21` the fields Task #3 introduced (`group_row`, `sub_header_row`, `first_data_row`, `skip_rows`, `derived_columns`, `external_links`) and the ratified blank rule (`blank cell inside a live row = 0` → `presence='zero_from_blank'`; pre-series label rows are not records). Also settle the CSV header mechanism: today `header_row` is **inert** on CSV paths (`loader21` CSV branch uses `csv_skip` + line 1; only the xlsx branch reads `header_row`) — pick one rule (extend the CSV path to read `header_row`) instead of duplicating state across curation | **OPEN** (filed 2026-09-11, surfaced by Task #3) | Evidence: `private/layouts/task3-proposal-2026-09-10.md` §2 F4; `tools/manifests/layouts.schema.md`; working CSV example `fi_balances:over-time` (`header_row: 3` + `csv_skip: 2`) |
| 19 | **Toolkit hardening pass** (findings filed 2026-09-11) — (a) make `cmd_query` read-only (`file:…?mode=ro`) so a stray `DROP` cannot destroy `books.db`; (b) fix the inspector's known blind spots: CSV header scorer counts numeric strings as labels, the `header unresolved` CLI warning is xlsx-only, and xlsx `--header-row` is applied workbook-globally; (c) fix `sheets._typed` rewriting any integer in the 20,000–80,000 range into a date (sent Task #3 legs after a phantom date column); (d) doc drift: stale `--skiprows` in the usage block and `tools/README.md`, `catalog.py` naming `catalog registry` vs the real `merge-registry`, `sheets grid`/`export` now documented | **PARTIAL — (a) DONE** (2026-09-11): `_connect_ro()` opens the store `mode=ro` for `query` and the finpage export; write attempts now refuse with a clear message (verified: `DROP`/`DELETE` → "attempt to write a readonly database", store intact); `assert_schema` made row-factory agnostic so the guard composes with any connection. Loader battery 30/30, both schema harnesses green, hygiene 2/2. **(b)(c)(d) OPEN** | Evidence: `.scratch/teamlab/verifier-report.md` (query D-B, reproduced by the COS); `.scratch/task3/legs/leg-a.md`/`leg-b.md`/`leg-c.md` |
| 20 | **Agent Teams trial — verdict + checkpoint** — experimental `teamlab` profile (plugins rc.2 against core rc.1, :3081), trialled against this repo's real work | **TRIAL RUN — sandbox-only** (verdict 2026-09-11) | Good: task board + `blocked_by` gate + CAS + `send_message` steering all worked; a verifier leg caught a material `query` defect (→#19a). Limits **within the Team path**: teammates expose no model override, so tool use and a cross-family leg stay mutually exclusive *there*; the session lease is single-writer across processes (never open one session on two instances). **Two premises SUPERSEDED 2026-09-12** (see #21): the 2026-09-10 finding that workflow `agent()` children cannot use tools is **retired**, and with model-pinned tool-using `subagent`/`workflow` legs the panel's independence rule **is** satisfiable without the plugin — the "unsatisfiable here" verdict applies to the Team path only, not to the panel. Team-path verdict itself unchanged: sandbox for independent verification, not the daily driver. **Checkpoint:** the first genuinely parallel job (#7's allow-list completion) or a plugin/core-aligned upgrade. Write-up: `~/Obsidian/AIML/dsh/dsh-agent-teams-experiment-2026-09-11.md` |
| 21 | **Panel capability: model-pinned, tool-using legs** — verify and record the 2026-09-12 dsh capability (subagent model selection; `workflow` `agent()` tool use) | **DONE — verified 4/4 routes** (2026-09-12) | All four flash routes pinned on `subagent`; every child quoted its own identity line verbatim and executed bash + read + glob. `workflow` `agent()` pinned two routes and used tools — proved by nonce files written to disk and confirmed from the COS shell. **Binding is per session at session start:** enabling `subagent-model-selection` mid-session, or restarting dsh and resuming an old conversation, fails with `child model selection is disabled for this tool instance`; the fix is a **new conversation**, not a restart. Recorded in `tools/PANEL.md` §3 ("Delegation doors") and `tools/panel_routes.json` `_policy.subagent_model_pinning`. Both previously parked workflow templates un-parked; their `DO NOT RUN YET` headers retired. Untested: `reasoning_effort` pinning. Architecture map + vocabulary: `~/Obsidian/AIML/dsh/dsh-subagent-architecture-2026-09-12.md` |

| 22 | **Structural pre-fetch artifact** — replace the loader's values-only CSV input with a structural artifact (values + formulas + merges + formats) so a **derived** cell can never enter the store indistinguishably from a **measured** one; absorbs **#18** (the loader consumes the Task #3 allow-list tiers). Blueprint `docs/superpowers/plans/2026-09-12-structural-prefetch-v0.3.md` | **v0.3 RATIFIED — P1 IN PROGRESS** (owner, 2026-09-12) | Evidence: `.scratch/csv_audit/AUDIT.md` (11 specs audited; at full extent 8 inadequate + 1 marginal — the first sweep ran in an 80-row window and understated the largest tabs, corrected in place). Two review rounds: 3 flash legs (all judged v0.1 "fails as written") + 4 pro reviewers (GPT-5.6 Sol · Grok 4.6 · Qwen3.8-Max reject; GLM-5.3-Max accept-with-changes) — unanimous that the design be consolidated into one v0.3; both impaired-independence disclosures are recorded in v0.3 §0. **Owner authorisation 2026-09-12: take the panel's recommendation on D-1** (derivation column-grain on the empty `src_column`, per-row `presence` only on families that need it). D-2 settled (keep `fi_balances` on CSV). Scope: first wave = `stats` + `ss-earning`; the five holdings specs are gated behind **#7** and a positional loader. Phases: **P1** artifact + completeness gate → **P0** schema → **P2** fresh-empty shadow → **P4** owner-reviewed reconciliation → **P3** WAL-safe promotion → **P5** CSV retirement |

## Owner rulings landed this session (2026-09-06, Roth review)

- "Effective Annuity (max)" semantics confirmed: goal-seek to ~zero at age 100 —
  a ceiling, not planned spending.
- Panel independence rule adopted: the COS-chair model is excluded from pro
  review of its own sessions (chair model per `tools/panel_routes.json`; since
  2026-09-08 that is `deepseek-flash`/V41-Flash, which also doubles as a
  priority-1 leg — every batch must carry a cross-family leg).

## Changelog

- **2026-09-04 (first COS):** bootstrap drafts — retired with intent (see
  `.scratch/retired/`), superseded by 2026-09-06 ratifications.
- **2026-09-06 (second COS):** data-architecture day — Drive-canonical +
  SQLite compute ratified; panel ratified; toolkit shipped; survey + schema
  v0.1 review completed.
- **2026-09-06 (Roth review):** Retirement Balance decoded; repo reviewed and
  corrected (routing table, layouts gaps, stale defect statuses, handoff
  figure-scrub, rejected schema moved, charter authored).
- **2026-09-08 (tax scan):** schema v0.2.4 (additive tax line-item columns,
  ALTER parity-verified against the script); 14 tax years loaded and
  panel-verified (4 flash legs, n/n); panel run logged at
  `.scratch/q2/panel/run-log.md`; Schedule C found blank in every fetched PDF.
- **2026-09-10 (Roth):** Task #3 proposal delivered — 8 tabs pre-fetched through
  the native-read path, 3 independent flash legs, convergence graded, COS
  spot-verification at source; 7 rulings requested
  (`private/layouts/task3-proposal-2026-09-10.md`). No allow-list, curation or
  store edit applied. Method note: workflow legs cannot use tools on dsh
  0.1.5-rc.1 (tool-using children return null) — both fan-out templates are
  annotated with the constraint; the working fan-out path is Team teammates.
- **2026-09-11 (Roth, structure day):** **Task #3 closed** — all 11 unresolved
  allow-list entries settled with the owner (`private/layouts/layouts.json`,
  0 unresolved). Enabling change: `bagend.py sheets grid` (native structure read:
  merges, formulas, formats; literals redacted by default) — a values-only read
  could not show a group band or a derived column, which is what had blocked the
  backlog. Also this day: the blank semantics were completed (blank cell in a
  live row = 0; absent row = not recorded; pre-series label rows = not records),
  the xlsx→Sheets migration defect family was swept and **certified clean across
  all 13 sheets** (`sheets errors --all`, D18), and the source flags D15–D17 were
  filed. Loader follow-through filed as Task #18. Agent Teams trial verdict
  recorded (#20, sandbox-only, vault write-up) and the method half committed and
  pushed as `b75ac23`.
- **2026-09-12 (Roth, capability day):** **model-pinned, tool-using fan-out is
  live.** `subagent` accepts `provider`/`model`/`reasoning_effort`, and all four
  flash routes were verified end to end — each child quoted its own identity line
  verbatim and executed bash + read + glob. Task **#19(a)** landed as `d094f91`
  (`_connect_ro()`; the query path can no longer write `books.db`). Two
  2026-09-10 method premises were **retired**: workflow `agent()` children *can*
  use tools (proved with nonce files on disk, not a printable success string), so
  both parked fan-out templates are runnable again; and with per-leg model pinning
  the panel's independence rule is satisfiable **without** the plugin — #20's
  "unsatisfiable here" verdict now applies to the Team path only. Recorded in
  `tools/PANEL.md` §3 ("Delegation doors") and `tools/panel_routes.json`
  `_policy.subagent_model_pinning`; ledger row #21. Also this day:
  `docs/JOURNEY.md` created; the 2026-09-10/11 daily-note gap backfilled in the
  vault; and the `session-catchup` skill's stale double-`--params` `gws tasks`
  invocation corrected (it errored `--params cannot be used multiple times` — all
  query parameters go in one JSON object).
- **Boundary log — owner-directed vault writes (AGENTS.md §5.4/§5.6).** Twice the
  owner has directly instructed writes into `~/Obsidian`, which §5.4 keeps
  read-only from this repo: **2026-09-11** (the Agent Teams write-up, logged in
  that session's handoff) and **2026-09-12** (the subagent architecture note at
  `~/Obsidian/AIML/dsh/dsh-subagent-architecture-2026-09-12.md`, plus the
  backfilled daily notes `2026-09-10.md` and `2026-09-11.md`). Each was a one-off,
  owner-directed exception logged per §5.6 — not a standing grant and not
  precedent for unattended vault writes. No other vault path was touched, and
  nothing financial was written.
